<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Dashboard Greenhouse') }}
        </h2>
    </x-slot>

    <div class="py-6 bg-gray-50 min-h-screen">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            
            @if(session('success'))
                <div class="bg-emerald-50 border-l-4 border-emerald-500 text-emerald-700 px-4 py-3 rounded mb-4 shadow-sm">
                    <p class="font-medium">✓ {{ session('success') }}</p>
                </div>
            @endif

            <!-- Status Online/Offline -->
            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg mb-6">
                <div class="p-4 flex items-center justify-between">
                    <h3 class="text-lg font-semibold text-gray-800">Status Sistem</h3>
                    @if($is_offline)
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-red-100 text-red-800">
                            <svg class="w-2 h-2 mr-1.5 animate-pulse" fill="currentColor" viewBox="0 0 8 8">
                                <circle cx="4" cy="4" r="3" />
                            </svg>
                            OFFLINE
                        </span>
                    @else
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-emerald-100 text-emerald-800">
                            <svg class="w-2 h-2 mr-1.5 animate-pulse" fill="currentColor" viewBox="0 0 8 8">
                                <circle cx="4" cy="4" r="3" />
                            </svg>
                            ONLINE
                        </span>
                    @endif
                </div>
            </div>

            <!-- Sensor Cards - Clean Design -->
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
                <!-- Suhu Udara -->
                <div class="bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200 overflow-hidden border border-gray-100">
                    <div class="p-5">
                        <div class="flex items-center justify-between mb-3">
                            <div class="flex items-center">
                                <div class="p-2 bg-red-50 rounded-lg">
                                    <svg xmlns="http://www.w3.org/2000/svg"
                                        class="w-6 h-6 text-red-600"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M14 14.76V3.5a2.5 2.5 0 10-5 0v11.26a4 4 0 105 0z"/>
                                    </svg>

                                </div>
                            </div>
                        </div>
                        <h6 class="text-gray-500 text-sm font-medium mb-2">Suhu Udara</h6>
                        <p class="text-3xl font-bold text-gray-900">
                            {{ $data ? number_format($data->temp_avg, 1) : '-' }}
                            <span class="text-lg text-gray-500">°C</span>
                        </p>
                        <small class="text-gray-400 text-xs">
                            {{ $data ? \Carbon\Carbon::parse($data->timestamp)->format('H:i:s') : 'No data' }}
                        </small>
                    </div>
                </div>

                <!-- Kelembaban -->
                <div class="bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200 overflow-hidden border border-gray-100">
                    <div class="p-5">
                        <div class="flex items-center justify-between mb-3">
                            <div class="flex items-center">
                                <div class="p-2 bg-blue-50 rounded-lg">
                                    <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z"/>
                                    </svg>
                                </div>
                            </div>
                        </div>
                        <h6 class="text-gray-500 text-sm font-medium mb-2">Kelembaban</h6>
                        <p class="text-3xl font-bold text-gray-900">
                            {{ $data ? number_format($data->hum_avg, 0) : '-' }}
                            <span class="text-lg text-gray-500">%</span>
                        </p>
                        <small class="text-gray-400 text-xs">
                            {{ $data ? \Carbon\Carbon::parse($data->timestamp)->format('H:i:s') : 'No data' }}
                        </small>
                    </div>
                </div>

                <!-- Suhu Air -->
                <div class="bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200 overflow-hidden border border-gray-100">
                    <div class="p-5">
                        <div class="flex items-center justify-between mb-3">
                            <div class="flex items-center">
                                <div class="p-2 bg-cyan-50 rounded-lg">
                                    <svg class="w-6 h-6 text-cyan-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707"/>
                                    </svg>
                                </div>
                            </div>
                        </div>
                        <h6 class="text-gray-500 text-sm font-medium mb-2">Suhu Air</h6>
                        <p class="text-3xl font-bold text-gray-900">
                            {{ $data ? number_format($data->temp_air, 1) : '-' }}
                            <span class="text-lg text-gray-500">°C</span>
                        </p>
                        <small class="text-gray-400 text-xs">Air Nutrisi</small>
                    </div>
                </div>

                <!-- Daya Kipas -->
                <div class="bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200 overflow-hidden border border-gray-100">
                    <div class="p-5">
                        <div class="flex items-center justify-between mb-3">
                            <div class="flex items-center">
                                <div class="p-2 bg-amber-50 rounded-lg">
                                    <svg class="w-6 h-6 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                                    </svg>
                                </div>
                            </div>
                        </div>
                        <h6 class="text-gray-500 text-sm font-medium mb-2">Daya Kipas</h6>
                        <p class="text-3xl font-bold text-gray-900">
                            {{ $data ? number_format($data->power, 0) : '-' }}
                            <span class="text-lg text-gray-500">W</span>
                        </p>
                        <small class="text-gray-400 text-xs">Power</small>
                    </div>
                </div>
            </div>

            <!-- Grafik & Kontrol -->
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
                <!-- Grafik -->
                <div class="lg:col-span-2 bg-white overflow-hidden shadow-sm sm:rounded-lg border border-gray-100">
                    <div class="p-6">
                        <h3 class="text-lg font-semibold text-gray-800 mb-4">Grafik Real-Time</h3>
                        <div class="relative" style="height: 350px;">
                            <canvas id="sensorChart"></canvas>
                        </div>
                    </div>
                </div>

                <!-- Status & Setpoint -->
                <div class="space-y-4">
                    <!-- Status Kipas -->
                    <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg border border-gray-100">
                        <div class="p-5 text-center">
                            <h6 class="text-sm font-semibold text-gray-700 mb-3">Status Kipas</h6>
                            @if($actuator && $actuator->fan_status == 1)
                                <span class="inline-flex items-center px-4 py-2 rounded-lg text-lg font-bold bg-emerald-100 text-emerald-800">
                                    <svg class="w-5 h-5 mr-2 animate-spin" fill="currentColor" viewBox="0 0 20 20">
                                        <path d="M10 2a1 1 0 011 1v1.323l3.954 1.582 1.599-.8a1 1 0 01.894 1.79l-1.233.616 1.738 5.42a1 1 0 01-.285 1.05A3.989 3.989 0 0115 15a3.989 3.989 0 01-2.667-1.019 1 1 0 01-.285-1.05l1.715-5.349L11 6.477V9a1 1 0 11-2 0V6.477L6.237 7.582l1.715 5.349a1 1 0 01-.285 1.05A3.989 3.989 0 015 15a3.989 3.989 0 01-2.667-1.019 1 1 0 01-.285-1.05l1.738-5.42-1.233-.617a1 1 0 01.894-1.788l1.599.799L9 4.323V3a1 1 0 011-1zm-5 8.274l-.818 2.552c.25.112.526.174.818.174.292 0 .569-.062.818-.174L5 10.274zm10 0l-.818 2.552c.25.112.526.174.818.174.292 0 .569-.062.818-.174L15 10.274z"/>
                                    </svg>
                                    ON
                                </span>
                            @else
                                <span class="inline-flex items-center px-4 py-2 rounded-lg text-lg font-bold bg-gray-100 text-gray-800">
                                    OFF
                                </span>
                            @endif
                            <p class="text-xs text-gray-500 mt-2">Auto ON > {{ $setpoint->suhu_setpoint ?? 30 }}°C</p>
                        </div>
                    </div>

                    <!-- Setpoint -->
                    <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg border border-gray-100">
                        <div class="p-5">
                            <h6 class="text-sm font-semibold text-gray-700 mb-3 text-center">Setpoint Saat Ini</h6>
                            <div class="grid grid-cols-2 gap-4">
                                <div class="text-center">
                                    <p class="text-xs text-gray-500 mb-1">Suhu</p>
                                    <p class="text-2xl font-bold text-emerald-600">{{ $setpoint->suhu_setpoint ?? '-' }}°C</p>
                                </div>
                                <div class="text-center">
                                    <p class="text-xs text-gray-500 mb-1">RH</p>
                                    <p class="text-2xl font-bold text-blue-600">{{ $setpoint->kelembaban_setpoint ?? '-' }}%</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Form Ubah Setpoint -->
            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg mb-6 border border-gray-100">
                <div class="p-6">
                    <h3 class="text-lg font-semibold text-gray-800 mb-4">Ubah Setpoint</h3>
                    <form method="POST" action="{{ route('setpoint.update') }}">
                        @csrf
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Batas Suhu (°C)</label>
                                <input type="number" name="suhu_setpoint" step="0.1" 
                                       value="{{ $setpoint->suhu_setpoint ?? 30 }}" 
                                       class="w-full rounded-lg border-gray-300 shadow-sm focus:border-emerald-500 focus:ring-emerald-500" required>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-2">Batas Kelembaban (%)</label>
                                <input type="number" name="kelembaban_setpoint" step="0.1" 
                                       value="{{ $setpoint->kelembaban_setpoint ?? 60 }}" 
                                       class="w-full rounded-lg border-gray-300 shadow-sm focus:border-emerald-500 focus:ring-emerald-500" required>
                            </div>
                        </div>
                        <div class="mt-4">
                            <button type="submit" class="bg-emerald-600 hover:bg-emerald-700 text-white font-semibold py-2.5 px-6 rounded-lg transition duration-150 shadow-sm hover:shadow-md">
                                Simpan Setpoint
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- History Table -->
            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg border border-gray-100">
                <div class="p-6">
                    <div class="flex justify-between items-center mb-4">
                        <h3 class="text-lg font-semibold text-gray-800">Data Terakhir</h3>
                        <a href="{{ route('download.csv') }}" class="bg-emerald-600 hover:bg-emerald-700 text-white font-semibold py-2 px-4 rounded-lg text-sm transition duration-150 shadow-sm hover:shadow-md">
                            <svg class="w-4 h-4 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                            </svg>
                            Download CSV
                        </a>
                    </div>
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Waktu</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Suhu</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">RH</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Air</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Watt</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">PLN</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Fan</th>
                                </tr>
                            </thead>
                            <tbody class="bg-white divide-y divide-gray-200">
                                @forelse($history as $row)
                                <tr class="hover:bg-gray-50">
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                        {{ \Carbon\Carbon::parse($row->timestamp)->format('H:i:s') }}
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">
                                            {{ number_format($row->temp_avg, 1) }}°C
                                        </span>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">
                                            {{ number_format($row->hum_avg, 1) }}%
                                        </span>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                        {{ number_format($row->temp_air, 2) }}°C
                                    </td>

                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                        {{ $row->power }} W
                                    </td>

                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                        {{ $row->pln_voltage }} V
                                    </td>

                                    <td class="px-6 py-4 whitespace-nowrap">
                                        @if($row->fan_status == 1)
                                            <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-emerald-100 text-emerald-800">
                                                ON
                                            </span>
                                        @else
                                            <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">
                                                OFF
                                            </span>
                                        @endif
                                    </td>
                                    </tr>
                                    @empty
                                    <tr>
                                        <td colspan="7" class="px-6 py-4 text-center text-gray-500">
                                            Tidak ada data
                                        </td>
                                    </tr>
                                    @endforelse
                                    </tbody>
                                    </table>
                        </div>
                    </div>
                </div>
            </div>
</div>

@push('scripts')
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    const ctx = document.getElementById('sensorChart').getContext('2d');
    new Chart(ctx, {
        type: 'line',
        data: {
            labels: @json($chartData->pluck('timestamp')->map(fn($t) => \Carbon\Carbon::parse($t)->format('H:i'))),
            datasets: [
                {
                    label: 'Suhu Udara (°C)',
                    data: @json($chartData->pluck('temp_avg')),
                    borderColor: '#ef4444',
                    backgroundColor: 'rgba(239, 68, 68, 0.1)',
                    tension: 0.4,
                    fill: true,
                    yAxisID: 'y'
                },
                {
                    label: 'Suhu Air (°C)',
                    data: @json($chartData->pluck('temp_air')),
                    borderColor: '#06b6d4',
                    backgroundColor: 'rgba(6, 182, 212, 0.1)',
                    tension: 0.4,
                    fill: true,
                    yAxisID: 'y'
                },
                {
                    label: 'Daya Kipas (Watt)',
                    data: @json($chartData->pluck('power')),
                    borderColor: '#f59e0b',
                    backgroundColor: 'rgba(245, 158, 11, 0.1)',
                    fill: true,
                    tension: 0.4,
                    yAxisID: 'y1'
                }
            ]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            interaction: {
                mode: 'index',
                intersect: false,
            },
            plugins: {
                legend: { 
                    position: 'top',
                    labels: {
                        usePointStyle: true,
                        padding: 15
                    }
                }
            },
            scales: {
                y: {
                    type: 'linear',
                    display: true,
                    position: 'left',
                    title: { display: true, text: 'Suhu (°C)' },
                    min: 0,
                    max: 50
                },
                y1: {
                    type: 'linear',
                    display: true,
                    position: 'right',
                    title: { display: true, text: 'Daya (Watt)' },
                    grid: { drawOnChartArea: false }
                }
            }
        }
    });

    // Auto refresh 30 detik
    setTimeout(() => location.reload(), 30000);
</script>
@endpush
</x-app-layout>